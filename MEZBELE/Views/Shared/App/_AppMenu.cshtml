<div class="ui inverted secondary pointing menu" id="AppMenu">
    <a class="active item" href="/">
        <i class="home icon"></i> <span id="menuName">Anasayfa</span>
    </a>
    <a class="item" href="/Issues/Index">
        <i class="tasks icon"></i> <span id="menuName">İşler</span>
    </a>
    <a class="item" href="/Crews/Index">
        <i class="users icon"></i> <span id="menuName">Ekipler</span>
    </a>
    <a class="item" href="/Projects/Index">
        <i class="suitcase icon"></i> <span id="menuName">Projeler</span>
    </a>
    <div class="right menu">
        <a class="ui item">
            <div class="ui inline dropdown">
                <div class="text">
                    <img class="ui avatar image" src="https://avatars1.githubusercontent.com/u/9089783">
                    <span id="userNameInfo">Nuri Uzunoğlu</span>
                </div>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div class="item" onclick="redirect('Index', 'Profile');">
                        <i class="user icon"></i> Profil
                    </div>

                    <div class="divider"></div>

                    <div class="item">
                        <i class="settings icon"></i> Ayarlar
                    </div>
                    <div class="item" onclick="redirect('LogOut', null);">
                        <i class="sign out icon"></i> Çıkış
                    </div>
                </div>
            </div>
        </a>
    </div>
</div>

<script type="text/javascript">
    $('.ui.dropdown').dropdown({
        transition: 'swing down',
        duration: 800,
    });

    function redirect(action, controller) {
        console.log("/" + controller + "/" + action);

        if (action != null && controller != null)
            window.location.href = "/" + controller + "/" + action;

        else if (action != null && controller == null)
            window.location.href = "/" + action;


        else if (action == null && controller != null)
            window.location.href = "/" + controller;
    }
</script>